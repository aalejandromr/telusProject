

                {%- for action in record_actions %}

                    
                        <a class="btn medium" href="{{ "{{ path('" ~ route_name_prefix ~ "_" ~ action ~ "', { 'id': entity.id }) }}" }}">{{ action }}</a> 
                    

                {%- endfor %}

